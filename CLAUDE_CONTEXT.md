# Claude Context — PWA Memo

This project is a simple offline-first PWA for memorization.
Constraints:
- Vanilla HTML / CSS / JS only
- No frameworks
- Offline-first (service worker)
- iOS Safari compatible
- GitHub Pages (project site)
- Dark mode, minimal UI
- One user, local storage only

The full PRD is provided in the prompt.
Do not invent features.
